version: "3.8"

services:
  alexa-ai:
    build: .
    container_name: alexa-ai
    ports:
      - "6301:6301"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
